"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var winston = require("winston");
var winston_1 = require("winston");
exports.logger = winston_1.createLogger({
    transports: [
        new winston.transports.Console({
            level: 'info',
            format: winston.format.combine(winston.format.colorize({ level: true }), winston.format.timestamp({
                format: 'HH:mm:ss.SSS'
            }), winston.format.printf(function (info) {
                return info.timestamp + " - " + info.level + ": " + (info.message ? info.message : '') + " " + (info.meta && Object.keys(info.meta).length ? JSON.stringify(info.meta) : '');
            }))
        })
    ]
});
//# sourceMappingURL=logger.js.map